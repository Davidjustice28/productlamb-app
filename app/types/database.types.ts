export enum BugGroup {
  ALL = "all",
  COMPLETED = "completed",
  DELETED = "deleted",
  MANUAL = "manual",
  EXTERNAL = "external"
}

export enum SettingsTabGroup {
  PREFERENCES = "preferences",
  NOTIFICATIONS = "notifications",
  BILLING = "billing",
}